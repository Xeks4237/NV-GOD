-- This file is loaded through lua/plugins/treesitter.lua

return {
    -- Initialize selection.
    init_selection = "gnn",

    -- Increment to the next node.
    node_incremental = "grn",

    -- Increment to the next scope (e.g., parent).
    scope_incremental = "grc",

    -- Decrement selection.
    node_decremental = "grm",
},

